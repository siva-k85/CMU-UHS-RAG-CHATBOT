# CMU Health Services RAG Chatbot - Environment Variables

# OpenAI Configuration
OPENAI_API_KEY=your-openai-api-key-here

# PostgreSQL Configuration
POSTGRES_PASSWORD=your-secure-password-here
SPRING_DATASOURCE_USERNAME=raguser
SPRING_DATASOURCE_PASSWORD=your-secure-password-here

# Redis Configuration
REDIS_PASSWORD=your-redis-password-here

# JWT Secret for Authentication (optional)
JWT_SECRET=your-jwt-secret-key-here

# Google Cloud Platform Configuration (for deployment)
GCP_PROJECT_ID=your-gcp-project-id
GCP_REGION=us-central1
GCP_ZONE=us-central1-a
GKE_CLUSTER_NAME=cmu-rag-cluster

# Docker Registry (for GCP deployment)
DOCKER_REGISTRY=gcr.io
DOCKER_IMAGE_TAG=latest

# Application Settings
NODE_ENV=production
SPRING_PROFILES_ACTIVE=docker

# Monitoring and Logging
SENTRY_DSN=your-sentry-dsn-here
DATADOG_API_KEY=your-datadog-api-key-here

# Email Configuration (optional, for notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Slack Integration (optional, for alerts)
SLACK_WEBHOOK_URL=your-slack-webhook-url

# Rate Limiting
RATE_LIMIT_REQUESTS_PER_MINUTE=60

# CORS Settings (comma-separated list for production)
CORS_ALLOWED_ORIGINS=https://your-domain.com,https://app.your-domain.com